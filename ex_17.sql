select sum(total) as SellingVolumeInDollars, e.firstname || ' ' || e.lastname as FullName from invoice join customer using (customerid) join employee as e on e.employeeid = customer.supportrepid group by SupportRepId;
