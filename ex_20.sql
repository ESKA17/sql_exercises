select sum(total) as SellingVolumeInUSD, e.firstname || ' ' || e.lastname as FullName from invoice join customer using (customerid) join employee as e on customer.supportrepid = e.employeeid group by SupportRepId order by SellingVolumeInUSD desc limit 1;
